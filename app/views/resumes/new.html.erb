<%= stylesheet_link_tag 'create_form', media: 'all', 'data-turbolinks-track': 'reload' %>

<%= form_with model: @resume, local: true do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle %>
  </div>

  <div>
    <%= form.label :pdf %>
    <%= form.file_field :pdf, accept: 'application/pdf' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>