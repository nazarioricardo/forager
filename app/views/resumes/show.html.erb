<h1><%= @resume.title %></h1>
<p><%= @resume.subtitle %></p>
<div>
<%= render 'application/link', text: "To Dashboard", path: dashboard_path %>
</div>
<%= link_to "Download PDF", url_for(@resume.pdf) if @resume.pdf.attached? %>